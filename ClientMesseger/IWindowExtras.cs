namespace ClientMesseger
{
    /// <summary>
    /// This interface makes sure that every Window implements a custom closing method.
    /// </summary>
    internal interface IWindowExtras
    {
        public void CloseWindow();
    }
}

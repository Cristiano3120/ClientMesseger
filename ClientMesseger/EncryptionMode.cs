namespace ClientMesseger
{
    /// <summary>
    /// Shows with which encryption mode data is send.
    /// </summary>
    internal enum EncryptionMode
    {
        None = 0,
        RSA = 1,
        AES = 2,
    }
}
